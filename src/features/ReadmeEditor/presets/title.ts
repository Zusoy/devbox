import { PresetSection } from '@/features/ReadmeEditor/section'

const Title: PresetSection = {
  name: 'Title and Description',
  content: `# Project Title
A brief description
  `
}

export default Title